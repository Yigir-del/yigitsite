# Vercel Blob
BLOB_READ_WRITE_TOKEN=""

# Vercel Postgres
POSTGRES_URL=""
DATABASE_URL=""

# Admin session — server only. NEVER prefix with VITE_.
# Generate SESSION_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_IDENTITY=""
ADMIN_PASSPHRASE=""
ADMIN_SESSION_SECRET=""
